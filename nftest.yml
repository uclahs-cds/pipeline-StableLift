---
global:
  temp_dir: test/work
  remove_temp: true
  clean_logs: true

cases:
  - name: SNV
    nf_script: ./main.nf
    nf_config: test/snv.config
    params_file: test/nftest.yaml
    skip: false
    verbose: true
